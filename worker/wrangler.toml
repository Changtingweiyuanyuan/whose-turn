name = "whose-turn-auth"
main = "src/index.ts"
compatibility_date = "2026-01-01"

# Custom domain 之後設定：auth.whoseturn.app
# routes = [{ pattern = "auth.whoseturn.app", custom_domain = true }]

# 部署前用 wrangler secret put 設定以下機密：
#   wrangler secret put LINE_CHANNEL_ID
#   wrangler secret put FIREBASE_PROJECT_ID
#   wrangler secret put FIREBASE_CLIENT_EMAIL
#   wrangler secret put FIREBASE_PRIVATE_KEY   # service account 的 PEM（含換行）
